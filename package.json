{
  "dependencies": {
    "broken-link-checker": "^0.7.8",
    "express": "^4.18.2"
  },
  "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "build": "npm install",
  "dev": "node index.js"  }
}
